// Metas operacionais do time de onboarding.
// Ajuste estes valores para refletir o pacto da operação.

export const METAS = {
  // % de deals com SLA <= SLA_PRAZO (30d) considerado saudável
  slaNoPrazo: 80,
  // % máximo aceitável de deals em faixa crítica (>30d) por operador
  maxCritico: 10,
  // tempo médio (dias) máximo aceitável por operador
  tempoMedioMax: 18,
};
