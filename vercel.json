{
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/cron/refresh", "schedule": "0 6 * * *" }
  ],
  "functions": {
    "api/restaurants.ts": { "maxDuration": 60 },
    "api/cron/refresh.ts": { "maxDuration": 60 }
  }
}
